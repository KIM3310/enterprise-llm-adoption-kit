apiVersion: v1
kind: Service
metadata:
  name: llm-backend
  namespace: llm-adoption
  labels:
    app: llm-backend
    part-of: enterprise-llm-adoption-kit
spec:
  type: ClusterIP
  selector:
    app: llm-backend
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
